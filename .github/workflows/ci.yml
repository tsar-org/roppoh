name: ci
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/setup-bun-turbo
      - uses: dprint/check@v2.3
      - run: bun biome ci
      - run: bun turbo check:type
      - run: bun knip
      - run: bun turbo build

  playwright-test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: lint-and-build
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/setup-bun-turbo
      - uses: ./.github/actions/setup-playwright
      - run: bun run test:visual
        working-directory: apps/roppoh
        continue-on-error: true
      - name: Upload VRT screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vrt-diff-screenshots
          path: |
            apps/roppoh/.vitest-attachments/**/*.png
            apps/roppoh/test/visual-regression/pages/__screenshots__/**/*.png
          retention-days: 7

  unit-test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: lint-and-build
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/setup-bun-turbo
      - run: bun run test:unit
        working-directory: apps/roppoh

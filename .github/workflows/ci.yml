name: ci
on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-bun
      - uses: ./.github/actions/setup-turbo
      - run: bun --bun oxfmt --check
      - run: bun --bun oxlint --deny-warnings --format=github
      - run: bun run type-check
      - run: bun turbo type-check
      - run: bun run knip
      - run: bun turbo build

  playwright-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: lint-and-build
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-buildx-action@v4
      - name: Run vrt docker container
        run: docker compose up -d
      - name: "roppoh vrt"
        run: docker compose exec linux-vrt sh -c "bun run --cwd apps/roppoh test:visual"
      - name: "ura-roppoh vtr"
        run: docker compose exec linux-vrt sh -c "bun run --cwd apps/ura-roppoh test:visual"
      - name: "neo-fujimatsu vrt"
        run: docker compose exec linux-vrt sh -c "bun run --cwd apps/neo-fujimatsu test:vrt"
      - run: docker compose down
      - name: Upload VRT screenshots
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: vrt-diff-screenshots
          path: |
            apps/roppoh/.vitest-attachments/**/*.png
            apps/roppoh/test/visual-regression/**/__screenshots__/**/*.png
            apps/ura-roppoh/.vitest-attachments/**/*.png
            apps/ura-roppoh/test/visual-regression/**/__screenshots__/**/*.png
            apps/neo-fujimatsu/test/vrt/**/*-snapshots/**/*.png
            apps/neo-fujimatsu/test-results/**/*.png
          retention-days: 7

  unit-test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: lint-and-build
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-bun
      - name: Test apps/neo-fujimatsu
        run: bun run test:e2e
        working-directory: apps/neo-fujimatsu

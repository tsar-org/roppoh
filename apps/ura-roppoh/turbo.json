{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src", "./*.ts"],
      "outputs": ["./dist/**"]
    },
    "cf-typegen": {
      "inputs": ["wrangler.jsonc", ".env.local"],
      "outputs": ["worker-configuration.d.ts"]
    },
    "type-check": {
      "inputs": ["src", "test", "$TURBO_ROOT$/tsconfig.json", "./*.ts", "./tsconfig.json"]
    }
  }
}

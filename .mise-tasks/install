#\!/usr/bin/env bash
#MISE description="Setup project environment"
#MISE depends=["clear-cache"]

mise install

bun install --frozen-lockfile

mise run env-decrypt

lefthook install

bunx playwright install

cd apps/roppoh && bun react-router typegen

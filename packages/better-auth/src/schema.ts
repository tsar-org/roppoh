import {
  account,
  invitation,
  member,
  organization,
  session,
  user,
  verification,
} from "./auth-schema";

export const schema = {
  account,
  invitation,
  member,
  organization,
  session,
  user,
  verification,
};
